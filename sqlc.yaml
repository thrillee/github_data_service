version: "2"
sql:
  - engine: "sqlite"
    queries: "internals/db/queries"
    schema: "./migrations"
    gen:
      go:
        out: "internals/db"
        emit_json_tags: true
        emit_prepared_queries: true
        emit_interface: true
        emit_exact_table_names: false
